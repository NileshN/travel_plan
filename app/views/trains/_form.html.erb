<%= form_for(@train) do |f| %>
  <% if @train.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@train.errors.count, "error") %> prohibited this train from being saved:</h2>

      <ul>
      <% @train.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code, :class => "input_field" %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :class => "input_field" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>